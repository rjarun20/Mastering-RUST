{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/hello_world/target/debug/hello_world",
            "args": [],
            "cwd": "${workspaceFolder}/hello_world",
            "stopOnEntry": false,
            "preLaunchTask": "rust: cargo build", // Ensure this matches the label in tasks.json
            "sourceLanguages": ["rust"]
        }
    ]
}